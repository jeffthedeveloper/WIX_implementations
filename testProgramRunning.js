let isProgramRunning = true; // Initially, set to true

if (!isProgramRunning) {
  // Start the program and set isProgramRunning to true
  isProgramRunning = true;
  console.log("Program started!");
} else {
  console.log("Program already running!");
}
